# include<stdio.h> //-->preprocessor director

int main(){
    int age = 22; 
    // printf("age is %d", age); 
    /* //--> format specifier
    for integers printf("age is %d", age);

    for real numbers printf("value of pie is %f", pi);

    for characters printf("star looks like this %c", star);
    */ //--> format specifier

    float pi = 3.14;
    // printf("value of pie is %f", pi);

    char hashtag = '#'; //--> character e always single quato '' dite hobe
    printf("hashtag looks like %c", hashtag);
    // printf("Hello Swarnali \n"); //--> \n is for next line
    
    return 0;
}